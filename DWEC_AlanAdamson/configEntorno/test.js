// Este código debería generar advertencias
var name = "Alan";  // ESLint debería advertir por usar 'var'

if (name == "Alan") {  // ESLint debería advertir por usar '=='
    console.log("Hola, Alan");  // ESLint debería advertir por usar 'console.log'
}
